# ---------------------------------------------------------------------------
# Tested-together versions for QLoRA fine-tuning on a 4 GB laptop GPU (Windows).
# Install PyTorch SEPARATELY first (see README) so you get the CUDA build,
# then run:  pip install -r requirements.txt
# ---------------------------------------------------------------------------
transformers==4.46.3
trl==0.12.2
peft==0.13.2
datasets==3.1.0
accelerate==1.1.1
bitsandbytes==0.44.1     # 4-bit quantization; has native Windows wheels from 0.44+
sacrebleu==2.4.3         # chrF / BLEU evaluation (good for short, low-resource text)
fastapi==0.115.5         # scripts/serve.py — HTTP contract LearnVoca's app expects
uvicorn==0.32.1          # ASGI server to run FastAPI
# requests is already pulled in transitively by transformers — no separate pin needed
